CREATE TABLE myTable (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    field1 INTEGER NOT NULL,
    field2 INTEGER NOT NULL
);

insert:
INSERT OR REPLACE INTO myTable(field1, field2) VALUES ?;

select:
SELECT *
FROM myTable
WHERE field2 = ? AND field1 IN ?;